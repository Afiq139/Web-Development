<!DOCTYPE html>
<html lang="en">
    <%- include('./partials/head.ejs') %>


<body>

    <!--go to nav.ejs for navigation-->
    <%- include('./partials/nav.ejs') %>

    
    <div class="blogs content">
        <h2>All Blogs</h2>

        <% if (blogs.length > 0) { %> <!--checking if blogs of length is equal to 0 or more-->
            <% blogs.forEach(blog => {%><!--cycling through the blogs -->

                <h3 class ="title"><%= blog.title %></h3>
                <p class ="snippet"><%= blog.snippet %></p>

            <% })%>
            <% } else {%>
                <p>There are no blogs to do display...</p>
        <% } %>
    </div>

    <%- include('./partials/footer.ejs') %>
</body>
</html>

